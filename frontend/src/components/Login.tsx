"use client";import{useState}from"react";import{api}from"@/lib/api";import type{Player}from"@/lib/types";
export function Login({onLogin}:{onLogin:(p:Player)=>void}){const[step,setStep]=useState<"phone"|"otp">("phone");const[phone,setPhone]=useState("");const[otp,setOtp]=useState("");const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");const[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{if(step==="phone"){const x=await api<{message:string}>("/auth/request-otp",{method:"POST",body:JSON.stringify({phone})});setMessage(x.message);setStep("otp");}else{const x=await api<{member:Player}>("/auth/verify-otp",{method:"POST",body:JSON.stringify({phone,otp})});onLogin(x.member);}}catch(e){setError(e instanceof Error?e.message:"Something went wrong.");}finally{setBusy(false);}}return <main className="center-page"><form className="login" onSubmit={submit}><div className="logo">🏸</div><div className="eyebrow">Private court · Members only</div><h1>{step==="phone"?"Ready to rally?":"Check WhatsApp"}</h1><p>{step==="phone"?"Enter your approved WhatsApp number to open the squad tracker.":`We sent a six-digit code for ${phone}.`}</p>{message&&<div className="notice">{message}</div>}{error&&<div className="notice error">{error}</div>}{step==="phone"?<div className="field"><label>WHATSAPP NUMBER</label><input autoFocus inputMode="tel" placeholder="98765 43210" value={phone} onChange={e=>setPhone(e.target.value)} required/></div>:<div className="field"><label>6-DIGIT OTP</label><div className="otp"><input autoFocus inputMode="numeric" maxLength={6} placeholder="••••••" value={otp} onChange={e=>setOtp(e.target.value.replace(/\D/g,""))} required/></div></div>}<button className="btn" disabled={busy}>{busy?"Please wait…":step==="phone"?"Send WhatsApp OTP":"Verify & enter"}</button>{step==="otp"&&<button type="button" className="link" onClick={()=>{setStep("phone");setMessage("");}}>Change number</button>}</form></main>}
