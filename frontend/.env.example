# Server-side proxy target. A previous NEXT_PUBLIC_API_BASE_URL value is also
# accepted by next.config.ts for backwards-compatible deployments.
BACKEND_API_URL=http://localhost:4000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
