const express=require("express");const cors=require("cors");const cookieParser=require("cookie-parser");const mongoose=require("mongoose");const {env}=require("./config/env");const {ok,fail}=require("./utils/api");
const app=express();app.set("trust proxy",1);app.use(cors({origin:env.clientOrigin,credentials:true}));app.use(express.json({limit:"100kb"}));app.use(cookieParser());
app.get("/api/health",(req,res)=>ok(res,{status:"ok",database:mongoose.connection.readyState===1?"connected":"disconnected"}));
app.use("/api/auth",require("./routes/authRoutes"));app.use("/api/matches",require("./routes/matchRoutes"));app.use("/api/players",require("./routes/playerRoutes"));app.use("/api/standings",require("./routes/standingsRoutes"));
app.use((req,res)=>fail(res,404,"NOT_FOUND","Route not found."));app.use((error,req,res,next)=>{console.error(error);if(error.name==="CastError")return fail(res,400,"INVALID_ID","Invalid identifier.");return fail(res,500,"INTERNAL_ERROR","Something went wrong.");});module.exports=app;
